# regular methods in class receive instance (self) as the first argument
# class methods take the class (cls) as the first argument
#   called from class and apply on class variable
# static methods do not pass anything by default

